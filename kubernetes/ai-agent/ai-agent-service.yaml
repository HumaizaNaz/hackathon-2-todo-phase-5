apiVersion: v1
kind: Service
metadata:
  name: ai-agent
  labels:
    app: ai-agent
spec:
  selector:
    app: ai-agent
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
